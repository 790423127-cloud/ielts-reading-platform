# IELTS Reading Platform 项目指令

## 如何使用本文件

本文件用于帮助你理解项目、判断风险和自主完成任务，不是要求机械执行的固定流程。

除“产品与安全底线”外，其余内容都是默认判断原则。应根据任务目标、影响范围和现有证据灵活取舍：简单问题快速处理，复杂或高风险问题深入调查。不要因为这里没有写出某个具体步骤就停止思考，也不要为了完成清单而进行无价值的操作。

## 项目目标

这是一个面向个人学习的 IELTS General Training Reading 平台，目前处于旧版替代验收阶段。

- 新版使用 Next.js、React 和 TypeScript 构建前端，使用 FastAPI 承载题库、学习记录、确定性判分和 AI 学习辅助。
- 旧版仍是功能、数据和交互对照基线，也是新版完成验收前的回退系统。
- 当前优先级是完成真实学习流程、保证旧新结果一致并提高可用性，不是追求炫技式架构或商业化功能。
- 订阅、支付、套餐、语音等商业化能力目前不在默认范围内，除非用户明确要求。

开始较大任务前，先阅读与任务直接相关的 `README.md`、`docs/ARCHITECTURE.md`、`docs/MIGRATION.md`、代码和测试。只读取真正有助于判断的内容，不必每次遍历整个仓库。

## 与用户协作

- 默认使用中文，先说明结论和对用户的实际影响，再解释技术原因。
- 用户不需要知道代码细节才能提出有效需求。主动把页面现象还原成可验证的功能目标。
- 发现用户的理解与实际代码、数据或运行结果不一致时，直接更正，并给出证据。
- 能从项目、旧版或运行结果中安全确认的事项直接推进，不把可自行调查的问题反问给用户。
- 只有缺少关键业务选择、操作有明显风险，或不同选择会大幅改变结果时才暂停询问。
- 不展示冗长内部推理，但要说明关键依据、实际验证和未确认内容。

## 自主工作原则

- 优先定位根因，避免只隐藏症状。
- 修改范围以“完整解决同一个问题”为边界，而不是以文件数量为边界。
- 可以跨前端、接口、领域逻辑、仓库层和测试追踪同一条调用链。
- 复用现有组件、接口、类型和设计语言；现有方案明显不合适时可以调整，但要控制影响并说明取舍。
- 信息不完整时，采用风险较低、容易回退的合理假设继续工作，并在结果中说明重要假设。
- 在多个方案之间综合考虑学习体验、正确性、维护成本、复杂度、性能和安全，不机械追求改动最少或技术最新。
- 不顺手重构、批量改名、统一格式、升级依赖或修复独立问题。
- 新实现完全替代旧实现时，检查引用后清理确认无用的重复分支；无法确认用途的旧代码先保留并说明。

## 项目结构与职责

- `apps/web`：Next.js 产品前端和用户交互。
- `services/api`：FastAPI 接口、业务服务、领域规则和数据访问。
- `packages/contracts`：前后端传输协议；共享协议变化时检查相关调用方。
- `docs`：架构、迁移计划和旧新对照依据。
- `scripts`：题库和学习数据迁移工具。

默认职责边界：

- Next.js App Router 负责前端路由。
- FastAPI 负责业务规则、答案、判分、Band、掌握度和学习证据。
- API 路由尽量只做输入输出编排，核心规则放在服务或领域层，数据访问通过仓库层完成。
- 共享协议描述传输结构，不承载判分算法。

这些边界用于防止规则分裂。局部实现方式可以根据现有代码自主选择，不必为了层次形式而增加无意义文件或抽象。

## 产品与安全底线

以下属于硬约束，除非用户明确决定改变产品安全模型：

1. 公开题目数据不得包含答案、判分依据或解析。
2. 标准答案、确定性判分和 Band 规则必须由后端决定。
3. 只有完整提交 40 题 GT Reading 后才能给出 Band 估分。
4. 完整模考进行中，后端必须拒绝 AI 教学帮助。
5. AI 只能解释后端提供的学习证据，不能修改答案、成绩、Band、任务状态或掌握度。
6. 学习记录应保留用户归属，不能破坏 `user_id` 隔离。
7. 数据库连接和真实数据读取不能在模块导入时产生副作用。
8. `.env` 和真实密钥不得写入源码、日志、测试快照或回复；配置示例只修改 `.env.example`。

如果任务会改变这些边界，应先说明目的、风险、数据影响和回退方案，获得用户确认后再执行。

## 旧版迁移与对照

- 旧版用于确认功能、题型、判分结果、数据含义和关键交互，不代表旧实现方式必须照搬。
- 不把旧版的路由补丁、版本补丁 CSS、全局 DOM 观察器、hash 监听或节点清理逻辑复制进新版。
- 迁移功能时，优先复现用户可感知行为和业务结果，再使用新版架构重新实现。
- 涉及答案归一化、判分、Band、题型或真实题库时，必须与冻结样例或旧版结果做针对性对照。
- 在桌面端和移动端验收完成前，不擅自移除旧版回退能力。

## 数据、AI 与外部操作

- 对题库、Session、学习记录和迁移清单保持保守；先做只读检查或预览，再决定是否写入。
- 学习数据迁移默认只运行预览。使用 `--apply`、执行回退删除、覆盖数据库或切换正式数据前必须征得用户确认。
- 自动测试使用模拟 AI 提供方，不调用真实付费接口。
- 不为了“验证一下”调用千问、DeepSeek、OpenAI 或其他可能计费的真实模型。
- 不静默切换 AI 供应商，也不在供应商配置缺失时伪装成功。
- 缓存、每日限额、调用审计和证据边界属于 AI 功能的一部分，修改相关逻辑时一并检查。

## 前端与交互

- 以真实学习流程是否顺畅为主要标准，兼顾桌面端和窄屏布局。
- 页面改动尽量沿用现有视觉语言和组件，不单独创造风格不一致的控件。
- 题型交互需考虑选择、填答、清除、拖拽或键盘操作、草稿恢复、提交后状态和错误反馈中与该题型相关的部分。
- 不通过全局 `hashchange`、`popstate`、整页 `MutationObserver` 或隐藏未知 DOM 节点来修补路由和界面。
- 涉及可视化或交互问题时，代码检查不能完全替代真实浏览器验证；优先复现用户路径，并查看页面错误和控制台错误。
- 自动化工具无法可靠验证人工操作时，如实说明限制，并给用户一个简短、明确的手动验证步骤。

## 后端与业务规则

- 对答案、分数、Band、掌握度、报告和学习计划等确定性结果，优先使用纯规则和可复现计算。
- 输入校验和失败处理应与实际风险相称，错误应清晰可定位，不要静默失败。
- 避免用大量 `if`、宽泛 `try/catch`、临时延迟、重复请求或无限重试掩盖根因。
- 修改公共接口或共享数据结构时，检查前端、测试、迁移脚本和其他直接调用方。
- 数据库或仓库层变化要考虑已有记录的兼容、备份和回退，不假设数据库永远为空。

## 保护现有工作

该项目经常存在多个连续任务留下的未提交修改。工作区不干净不是停止工作的理由，但必须：

- 先检查 Git 状态和与任务相关的差异；
- 把现有修改视为用户成果，不覆盖、不回退、不整仓格式化；
- 只编辑本任务需要的区域，遇到重叠时先理解现有意图；
- 不使用 `git reset --hard`、强制检出、清理整个工作区等破坏性命令；
- 临时构建产物和缓存可以在确认可再生、路径准确且不含用户数据后处理。

## 验证策略

验证应与风险相称，由你根据改动自主组合，不要求每次运行全部检查。

常用入口：

```powershell
pnpm typecheck:web
pnpm test:web
pnpm build:web
pytest services/api/tests -q
```

一般判断：

- 文案或低风险样式：检查相关页面和差异即可，必要时做窄屏验证。
- 前端逻辑或题型交互：运行相关测试、类型检查，并尽量走一次真实浏览器流程。
- 后端业务规则：运行最接近该规则的 Pytest；影响公共规则时扩大到相关测试组。
- 判分、Band、答案或题库：增加旧新对照或冻结样例验证。
- 跨前后端协议：同时检查调用方、类型和接口实际响应。
- 构建或全量测试成本合理且改动影响广时，可以运行；局部改动不必机械跑全套。

如果检查失败，先判断是否由本次修改引起。由本次修改造成的应继续修复；既有问题要与本次结果分开说明。没有实际运行的检查不能声称通过。

运行生产构建前注意它可能与正在运行的 Next.js 开发服务器共用 `.next`。避免为了验证破坏用户当前页面；如果发生缓存冲突，只处理确认可再生的缓存和本项目进程，并恢复可用状态。

## 必须先确认的事项

执行以下操作前向用户说明准备做什么、最坏影响、费用或数据风险以及回退方式：

- 删除、覆盖、迁移真实题库或学习数据；
- 对正式数据库执行写入、切换或回退；
- 修改认证、权限、支付、计费或用户数据隔离模型；
- 调用真实付费 AI 或其他会产生费用的服务；
- 上传私密文件，或代表用户发送、发布、购买、提交线上内容；
- 提交、推送、合并、创建拉取请求或部署；
- 破坏性 Git 操作；
- 更换框架、数据库、认证系统或核心依赖；
- 大规模重构，或把任务扩展成新的独立项目。

阅读文件、搜索代码、本地修改、只读诊断、针对性测试和普通构建可直接进行。

## 完成与汇报

只要没有真实阻塞，就持续工作到当前目标被完整处理，而不是停在表面修补。

完成时按本次任务实际情况简洁说明：

- 根因或目标；
- 修改了什么，以及对用户有什么影响；
- 删除了什么、保留了什么及原因（没有则不必逐项凑格式）；
- 实际运行的检查和结果；
- 尚未确认的内容、剩余风险和最值得用户亲自验证的一步；
- 如何回退本次修改。

如果没有发生数据写入、费用、外部账号操作、发送、发布、提交、上传或部署，用一句话统一说明即可。
