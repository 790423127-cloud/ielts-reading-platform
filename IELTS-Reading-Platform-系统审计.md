# IELTS Reading Platform 系统审计指令

## 审计目标

对 IELTS General Training Reading 平台进行一次系统审计，找出真实功能缺陷、旧新迁移差异、判分与 Band 风险、前后端规则分裂、重复实现、遗留补丁、数据安全问题和验证盲区。

本次审计的目标是形成可靠的修复依据，不是为了凑检查项目，也不默认立即重构整个项目。

你拥有充分自主权，可以：

- 阅读与任务相关的代码、文档和测试；
- 追踪前端、API、领域逻辑、仓库层和数据库调用链；
- 运行本地测试、构建和浏览器验证；
- 使用冻结样例、mock、fixture 或临时数据库；
- 对照旧版和新版行为；
- 自主决定审计顺序和深度。

默认只读审计。不要直接修改真实题库、学习记录、正式数据库、用户数据或线上环境。

---

## 一、先确认项目真实结构

先阅读：

- `AGENTS.md`
- `README`
- `docs/ARCHITECTURE.md`
- `docs/MIGRATION.md`
- 前后端依赖和启动方式
- 与主要学习流程、判分、Band、AI 和迁移直接相关的代码与测试

确认：

- `apps/web` 的真实路由和用户入口；
- `services/api` 的接口、服务、领域规则和仓库层；
- `packages/contracts` 的共享协议；
- `scripts` 中的数据迁移工具；
- 旧版的实际运行入口和冻结对照样例；
- 新旧系统之间的数据与行为对应关系。

不要仅根据目录名或组件名推断职责，必须追踪真实调用链。

---

## 二、真实学习流程审计

完整走查 IELTS GT Reading 的主要用户路径：

1. 进入练习或模考；
2. 加载文章和题目；
3. 选择、填答、拖拽或匹配；
4. 保存草稿；
5. 切换题目和文章；
6. 刷新或重新进入；
7. 提交答案；
8. 查看判分、解释和报告；
9. 查看学习记录、掌握度和学习计划；
10. 在允许条件下使用 AI 教学帮助。

检查：

- 页面状态是否与后端 Session 一致；
- 草稿恢复是否可靠；
- 刷新、返回、切换设备或重复提交是否产生异常；
- 未完成 40 题时是否错误显示 Band；
- 提交后是否还能修改不应修改的内容；
- 错误状态是否清晰；
- 桌面端和移动端流程是否一致；
- 题目切换、定位、滚动和键盘操作是否正常；
- 旧版可用功能是否在新版遗漏或行为改变。

---

## 三、题型交互审计

按项目实际支持的题型分别检查，例如：

- 单选；
- 多选；
- 判断题；
- 填空；
- 匹配；
- 段落信息匹配；
- 标题匹配；
- 句子完成；
- 摘要或表格填空；
- 拖拽类题型。

每种题型至少检查：

- 初始状态；
- 作答；
- 修改答案；
- 清除答案；
- 键盘操作；
- 移动端操作；
- 草稿保存与恢复；
- 提交后的锁定状态；
- 错误反馈；
- 公开题目数据是否泄露答案或判分依据；
- 新旧版行为是否一致。

重点寻找多个题型各自实现相似状态逻辑而产生的不一致。

---

## 四、答案与确定性判分审计

这是高优先级审计。

确认：

- 标准答案只由后端决定；
- 公开题目数据不包含答案、解析或判分依据；
- 前端不能自行决定正确答案、分数或 Band；
- 答案归一化规则一致且可复现；
- 大小写、空格、标点、连字符、数字形式和允许变体处理正确；
- 多选题、匹配题和顺序题的判分规则正确；
- 重复提交不会产生不同结果；
- 判分结果与旧版冻结样例一致；
- 异常输入不会导致误判或静默通过；
- 判分代码没有被 AI 结果影响。

对关键规则使用冻结样例或旧版结果做针对性对照。

---

## 五、Band 估分审计

确认：

- 只有完整提交 40 题 GT Reading 后才给出 Band；
- 正确题数到 Band 的映射来源明确且唯一；
- 前后端不存在两套 Band 规则；
- 未完成、练习模式和完整模考的显示逻辑正确；
- 边界分数和异常分数处理正确；
- 旧版与新版 Band 结果一致；
- AI 不能修改或覆盖 Band；
- 报告中显示的分数、正确数和 Band 相互一致。

---

## 六、AI 学习辅助审计

确认 AI 只解释后端提供的学习证据，不参与确定性结果。

检查：

- 完整模考进行中，后端是否真正拒绝 AI 教学帮助；
- 前端隐藏按钮是否被错误当成唯一保护；
- AI 是否可能获得标准答案或未提交题目的判分依据；
- AI 是否可能修改答案、成绩、Band、任务状态或掌握度；
- 学习证据是否来自后端且有用户归属；
- 缓存、每日限额、调用审计是否正确；
- 供应商配置缺失时是否清晰失败；
- 是否可能静默切换 AI 供应商；
- 自动测试是否调用真实付费模型；
- 重复请求、重试和中止逻辑是否安全；
- AI 返回是否被错误写回确定性数据。

不得调用真实付费 AI 进行审计。

---

## 七、用户隔离与数据安全审计

检查：

- 所有学习记录、Session、报告、证据和掌握度是否绑定正确 `user_id`；
- 前端传入的用户标识是否被后端盲目信任；
- 仓库查询是否始终包含用户范围；
- 是否存在读取或修改其他用户数据的可能；
- 测试是否覆盖用户隔离；
- 缓存键是否包含用户维度；
- 数据库连接和真实数据读取是否在模块导入时产生副作用；
- 日志、快照和错误响应是否泄露私密数据；
- `.env`、密钥和连接字符串是否安全。

---

## 八、前后端协议与规则分裂审计

检查：

- `packages/contracts` 与真实 API 响应是否一致；
- 前端类型是否掩盖后端实际字段差异；
- 公共协议变化是否遗漏调用方；
- API 路由是否承载了过多业务规则；
- 判分、Band、掌握度等规则是否在前端和后端重复实现；
- 仓库层是否泄漏业务规则；
- 数据结构是否存在旧新字段并存；
- 错误响应是否稳定且可理解；
- 兼容旧数据时是否存在静默默认值。

---

## 九、旧版迁移与回退审计

确认：

- 旧版是行为和数据对照，不是实现方式模板；
- 新版没有复制旧版的全局 DOM 观察器、hash 补丁、节点清理和版本补丁 CSS；
- 已迁移功能的用户行为与旧版一致；
- 尚未完成的迁移项目有明确状态；
- 旧版回退入口仍可用；
- 移除旧版前是否具备桌面端和移动端验收证据；
- 迁移脚本默认只预览；
- `--apply`、回退删除和正式数据切换不会被误触；
- 数据迁移可重复、可中止、可回退；
- 旧版和新版数据含义没有错位。

---

## 十、前端代码与交互遗留审计

重点寻找：

- 多套相似页面或题型组件；
- 重复状态；
- 重复事件监听；
- 临时延迟；
- 宽泛 `try/catch`；
- 全局 `hashchange`、`popstate` 或整页 `MutationObserver` 补丁；
- 隐藏未知 DOM 节点的修复方式；
- 新旧路由并存且互相覆盖；
- 仅为测试通过而保留的特殊判断；
- 移动端和桌面端行为不一致；
- 页面显示成功但后端实际失败；
- 错误被静默吞掉；
- 已失效但仍被引用的旧组件或兼容层。

不要未经引用检查就建议删除。

---

## 十一、后端、数据库与迁移审计

检查：

- API 路由、服务、领域层和仓库层职责是否清楚；
- 数据库查询是否存在遗漏用户范围、N+1 或错误事务边界；
- 重复提交和并发更新是否安全；
- Session 状态转换是否合法；
- 失败后是否留下半写入数据；
- 数据库不为空时迁移是否仍安全；
- 备份和回退路径是否真实可用；
- 模块导入是否建立连接或读取真实数据；
- 错误处理是否清楚而非静默失败；
- 是否用大量条件、重复请求或无限重试掩盖根因。

---

## 十二、测试有效性审计

检查测试是否真正覆盖用户和业务风险：

- 是否只测组件，没有走完整学习流程；
- 是否只测前端显示，没有验证后端状态；
- 是否只测 API 返回，没有验证数据库变化；
- 是否覆盖 40 题完整提交；
- 是否覆盖 Band 边界；
- 是否覆盖答案归一化；
- 是否覆盖用户隔离；
- 是否覆盖 AI 禁用条件；
- 是否覆盖重复提交、刷新恢复和异常输入；
- 是否覆盖移动端关键交互；
- 是否对照旧版冻结样例；
- 是否存在测试绿但真实浏览器流程失败；
- 是否把未运行或被阻止的检查写成通过。

根据风险运行最有价值的定向测试，不机械运行所有检查。

---

## 十三、审计报告格式

按风险等级输出：

### P0：答案泄露、错误判分、用户数据越权、真实数据损坏或费用风险

### P1：核心学习流程不可用、Band 错误、旧新结果不一致

### P2：重复实现、迁移遗留、规则分裂、测试盲区和维护风险

### P3：低风险体验、文案、布局或代码质量问题

每个问题必须包含：

- 问题名称；
- 已确认事实；
- 证据位置；
- 用户实际影响；
- 根本原因；
- 建议修复方案；
- 预计影响范围；
- 建议验证方式；
- 是否需要用户确认；
- 不修复的风险。

同时给出：

1. 项目整体结论；
2. 最优先处理的 3—5 个问题；
3. 可以合并修复的问题；
4. 不建议当前阶段处理的问题；
5. 证据不足的疑似问题；
6. 推荐修复顺序；
7. 旧版替代验收的当前完成度；
8. 建议补充的关键测试；
9. 建议清理的重复实现或兼容层候选。

---

## 十四、审计边界

审计过程中不得：

- 修改真实题库和学习数据；
- 对正式数据库执行写入；
- 调用真实付费 AI；
- 提交、推送、创建 PR、部署或发布；
- 删除旧版回退能力；
- 执行破坏性 Git 操作；
- 因发现问题而直接大规模重构。

可以：

- 阅读和搜索代码；
- 运行只读诊断；
- 使用 mock、fixture、冻结样例和临时数据库；
- 运行本地测试和构建；
- 打开本地浏览器执行真实学习流程；
- 输出修复方案和候选差异。

没有实际检查或验证的内容，必须标为“尚未确认”，不得写成已确认问题。
